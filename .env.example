# Porta onde o programa fica disponível (http://localhost:PORTA).
PORT=5175

# Pasta onde o salarios.xlsx fica guardado. Por padrão é a pasta "dados"
# dentro do próprio programa — só troque se quiser usar outro local.
# DADOS_DIR=C:\caminho\para\pasta
